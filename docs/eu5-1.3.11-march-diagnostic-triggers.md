# EU5 1.3.11 march 只读诊断 trigger

## 目标、范围与非目标

为 Steam Build `24187685` / `eu5.exe` SHA-256
`c0db888da5e132cd6ab50c2c531c7cae419488bea0054cf8ac348616332683ef`
补充 `country_rank = country_rank:rank_county` 与
`subject_type_is_not_locked = yes` 的最小 trigger 覆盖，使外置 Phase 2 验收夹具能把
`march` 聚合失败拆成玩家可见的 rank 与类型锁定子条件。

本切片只检查 opcode 是否出现在 trigger 侧，不求值、不模拟 rank 比较或附属类型锁定，
也不证明 `make_subject_of` 的运行时结果。`VALIDATED` 不能替代简中 EU5 实机证据。

## exact-build 依据

- `game/in_game/common/subject_types/march.txt` SHA-256
  `ad6c5b7869c067b5a2a07961a0f3ee2a7210b5aa43619e871211f87a936fd40a`：
  `visible` 在目标 country scope 使用 `subject_type_is_not_locked = yes`，并比较宗主与目标的
  `country_rank_level`。
- `game/in_game/events/king_in_prussia.txt` SHA-256
  `1f786eda7f174428f7bfb43b125cc94fbaa136104957d1a5d8b650ee788c3b03`：
  country-event trigger 使用 `country_rank = country_rank:rank_county`。

## 设计与验收标准

1. 两个 opcode 仅登记为 EU5 exact-build profile 的 trigger；在 event option 的
   `trigger` 与 `c:<tag>` country scope 中通过。
2. 在 effect 侧使用必须得到 `WRONG_DOMAIN`；相似但未登记的 opcode 继续
   `UNKNOWN_OPCODE`，维持失败闭锁。
3. Phase 2 完整 fixture、产品脚本与已有 EU5 profile 测试必须保持 0 个 semantic error。
