# EU5 1.3.11 当前附属类型锁定 effect

## 目标、范围与非目标

为 Steam Build `24187685` / `eu5.exe` SHA-256
`c0db888da5e132cd6ab50c2c531c7cae419488bea0054cf8ac348616332683ef`
补充 `lock_current_subject_type = yes` 的最小静态 effect 覆盖，使验收夹具可在
country scope 中准备已经锁定当前附属类型的特殊附属国。

本切片只识别该 effect 的 trigger/effect 位置，不模拟锁定状态、附属类型升级、
冷却或后续 `make_subject_of` 行为；`VALIDATED` 不代表运行时锁定成功。

## exact-build 依据

`game/in_game/common/country_interactions/samanta_upgrades.txt` SHA-256
`382f27f0672ddc89263b34374d0c509679c55d5a121f110702f5179037358020`：

- `effect -> scope:recipient` 中两次使用 `lock_current_subject_type = yes`；
- 随后分别执行 `make_subject_of` 切换至 `maha_samanta` 与
  `pradhana_maha_samanta`，证明其为 country-scope effect，而非 trigger 或
  interaction 声明字段。

## 设计与验收标准

1. 将 `lock_current_subject_type` 登记为 EU5 exact-build profile 的 effect；
   scalar `yes` 形态在 event option/country scope 中通过。
2. 在 event trigger 中使用必须得到 `WRONG_DOMAIN`；未知相似名称继续
   `UNKNOWN_OPCODE`。
3. 完整 Phase 2 fixture 必须 syntax=0、semantic=0；正式 scripted trigger 与
   country interaction 不回退；仓库级静态验收通过。
