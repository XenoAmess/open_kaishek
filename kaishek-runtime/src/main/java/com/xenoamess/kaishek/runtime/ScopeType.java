package com.xenoamess.kaishek.runtime;

/** Typed scope kinds understood by the finite runtime. */
public enum ScopeType {
    ROOT, THIS, PREV, CHARACTER, TITLE, ORGANIZATION, PERFORMANCE_DOMAIN,
    PROVINCE, COUNTY, CASE, GLOBAL, SAVED, EVENT, DECISION, INTERACTION,
    ACTIVITY, WAR, ARMY, COUNCIL, ITERATOR
}
